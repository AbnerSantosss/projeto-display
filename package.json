{
  "name": "projeto-display",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma db push && npm run build",
    "start": "cd backend && npm start"
  }
}
