{
  "name": "projeto-display",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push --accept-data-loss && npm start",
    "seed": "cd backend && npx prisma db seed"
  }
}
